# 美团 AI Hackathon 交付方案：复杂指令下的多轮对话评测系统

## 一句话定位

把“外呼模型是否遵循复杂任务指令”转化为一个可复现的闭环：**官方指令导入 → 多样化用户仿真 → 证据锚定逐项判定 → 统计可靠性 → 失败诊断与改进建议**。

## 科学问题

复杂外呼任务的失败不是单轮回答错误，而是多轮条件分支中出现：流程跳步、用户打断后恢复失败、信息一次性倾倒、硬约束违规、用户没有真正理解。

- 假设 H1：参数化 persona 能比单一“配合用户”更有效暴露流程和约束问题。
- 实验 E1：对同一指令生成配合/犹豫/拒绝/追问/开车/分心/不满/低理解力等对话，比较维度分和失败项。
- 预期结果：低接受度、高紧迫性、低理解力 persona 会显著降低 flow/info/task，暴露真实外呼中的核心风险。

- 假设 H2：冻结的原子化 rubric + 证据锚定能提升评测可解释性和一致性。
- 实验 E2：同一批对话多次评测，报告 BCa CI、ICC、Gwet’s AC1、Krippendorff’s α、逐项稳定性。
- 预期结果：高稳定项可自动采信，不稳定项进入人工抽检/校准池。

## 系统亮点

1. **官方 Excel 零依赖导入**：`src/instruction_loader.py` 可直接读取赛题 xlsx。
2. **多样化用户模拟器**：persona 参数覆盖支配性、接受度、紧迫性、理解力。
3. **Simulator Quality Gate**：自动检查元评论泄漏、过长回复、过度配合、persona 不一致。
4. **Agent-as-a-Judge**：不是一次性打分，而是先编译 rubric，再逐项判定。
5. **证据锚定**：每个判定附对话原文，证据无法验证则降级。
6. **确定性约束校准**：字数、禁用词、开场白覆盖等用代码检查。
7. **统计可靠性**：报告均值、标准差、BCa 置信区间和一致性指标。
8. **失败诊断**：输出 Top 失败项、证据、原因、瓶颈维度、可执行改进建议。

## 演示命令

```bash
# 全量评测官方 Excel
python src/eval_v3_runner.py \
  --instruction-file "命题二：外呼任务对话模型指令示例 (1).xlsx" \
  --n-dialogues 8 \
  --n-runs 5

# 快速演示单条任务
python src/eval_v3_runner.py \
  --instruction-file data/instructions.json \
  --instruction-id 2 \
  --n-dialogues 2 \
  --n-runs 2
```

## 评委视角的答辩主线

1. 复杂指令评测的本质是“多轮过程质量评估”，不是最终一句话准确率。
2. 我们用 persona simulation 主动制造真实外呼中的干扰：忙、开车、追问、拒绝、分心、低理解力。
3. 我们用冻结 rubric 和证据锚定解决 LLM judge 不可解释的问题。
4. 我们用确定性检查和统计一致性解决“评测是否可靠”的问题。
5. 最终报告不只给分数，还告诉业务方“坏在哪里、证据是什么、下一步怎么修”。
